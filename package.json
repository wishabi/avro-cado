{
  "name": "@flipp/avro-cado",
  "version": "0.1.1",
  "description": "Flipp Avro Encoder Decoder",
  "main": "release/index.js",
  "typings": "release/index",
  "author": "Flipp",
  "scripts": {
    "test": "jest",
    "package": "(REL_DIR='./release/'; git checkout HEAD -- ${REL_DIR} && git rm -rf ${REL_DIR} && tsc && git add ${REL_DIR})",
    "build": "(REL_DIR='./release/'; tsc)"
  },
  "devDependencies": {
    "@types/jest": "^23.3.1",
    "@types/node": "7.0.53",
    "jest": "^23.5.0",
    "ts-jest": "^23.1.4",
    "tslint": "^5.11.0",
    "typescript": "3.0.1"
  },
  "dependencies": {
    "avsc": "5.0.6",
    "axios": "^0.18.0"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com:wishabi/Flipp-sr-enc-dec.git"
  },
  "jest": {
    "collectCoverage": true,
    "collectCoverageFrom": [
      "!ts/examples/**",
      "!ts/index.ts",
      "ts/**/*.{ts}"
    ],
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "testRegex": "(/tests/.*|\\.test)\\.(ts|tsx|js)$",
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js",
      "json"
    ]
  },
  "unmockedModulePathPatterns": [
    "<rootDir>/node_modules"
  ],
  "moduleDirectories": [
    "<rootDir>/node_modules",
    "<rootDir>/ts"
  ]
}
